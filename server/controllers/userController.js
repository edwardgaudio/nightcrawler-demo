
/**
*  @module userController
*  @desc This is not used yet, user system is not for a while on the roadmap,
*    just houses index and some examples for now
**/

const userController = {
  getUser(req, res) {
    return res.status(501).send('Not yet implimented.');
  },
};

module.exports = userController;
